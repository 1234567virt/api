localhost/read/- (get)/возвращает определенное количество записей объект json  
localhost/read_one/-(get) ?id=значения число запрос по полю id  /возвращает один json объект
localhost/search/-(get)  выполняет поиск по бд ?s=значения число(ид) или дата или текст(продукт) / возвращает объекты json или ошибку 404  
localhost/delete/- (post) объект json {"id": число} /результат выполнения
localhost/update/-нереализован
localhost/create/-(post) объект json {"id":число,"product":текст} короче все данные таблицы-/Результат должен быть создания записи в бд.контроллер необязательный  