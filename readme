localhost/product/read/- (get)/возвращает определенное количество записей объект json  
localhost/product/read_one/-(get) ?id=значения число запрос по полю id  /возвращает один json объект
localhost/product/search/-(get)  выполняет поиск по бд ?s=значения число(ид) или дата или текст(продукт) / возвращает объекты json или ошибку 404  
localhost/product/delete/- (post) объект json {"id": число} /результат выполнения
localhost/product/update/-нереализован
localhost/product/create/-(post) объект json {"id":число,"product":текст} короче все данные таблицы-/Результат должен быть создания записи в бд.контроллер необязательный  